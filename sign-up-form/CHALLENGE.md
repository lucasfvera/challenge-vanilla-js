# Sign Up Form

Build a user sign up form with the following features.

- An email and a password input
    - Email must have an “@” and the domain side must include a “.”
- Password must include
    - at least one special character
    - one number
    - be at least 8 characters
- Submission request handling
- Use a mock API function to handle submissions
- Basic aesthetics with pure CSS